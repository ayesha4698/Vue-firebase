#!/bin/bash

# Make sure you first create the conda environment by running the command:
# conda create -n cs4670 python=3.6
# Then, run this script inside the environment you created

conda install -c anaconda tk
pip install numpy
pip install opencv-python
pip install Pillow